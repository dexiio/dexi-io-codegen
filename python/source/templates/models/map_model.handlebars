# -*- coding: utf-8 -*-
from dexi.models.map_model import MapModel

class {{model.className}}(MapModel):
    def __init__(self, args=None, **kwargs):
        """{{model.description}}"""
        MapModel.__init__(self, **kwargs)
